#!/usr/bin/with-contenv bash

conf_dir="/epgd/config"

# make folder
[[ ! -d $conf_dir ]] && \
	mkdir -p $conf_dir

# copy config
[[ -d $conf_dir ]] && \
	cp --no-clobber --recursive /defaults/config/. $conf_dir/
